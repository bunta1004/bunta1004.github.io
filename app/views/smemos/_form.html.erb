<%= form_for(smemo) do |f| %>
  <% if smemo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(smemo.errors.count, "error") %> prohibited this smemo from being saved:</h2>

      <ul>
      <% smemo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_area :name %>
  </div>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_area :title %>
  </div>

  <div class="field">
    <%= f.label :memo %>
    <%= f.text_area :memo %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
